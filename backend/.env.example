# Gemini API キー。無くても動作します(未設定時は usedAI:false のモック結果を返す)。
# 実際のキーは backend/.env に入れてください(.env は .gitignore 済み = コミットされません)。
GEMINI_API_KEY=your_gemini_api_key_here

# 使用する Gemini モデル(任意)。既定は gemini-2.5-flash。
GEMINI_MODEL=gemini-2.5-flash
